{
  "name": "pcklinik-dk",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "preview": "node build.mjs && npx --yes serve dist"
  },
  "dependencies": {
    "worker-mailer": "^1.2.1"
  }
}
